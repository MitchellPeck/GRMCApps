{
  "name": "meeting-minutes",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "//test": "config.ts demands the DB env vars at import time, so every test file needs them present even though only the TEST_DATABASE_URL tests open a connection. Set TEST_DATABASE_URL to include the database-backed tests, and install jsdom (npm i -D jsdom) to include the browser ones; both skip without.",
    "test": "tsc && MEETINGMINUTES_DB_USER=test MEETINGMINUTES_DB_PASSWORD=test MEETINGMINUTES_DB_NAME=test node --test --test-concurrency=1 \"dist/*.test.js\""
  },
  "dependencies": {
    "@fastify/multipart": "^9.0.1",
    "@fastify/static": "^8.0.3",
    "fastify": "^5.1.0",
    "pg": "^8.13.1",
    "sherpa-onnx-node": "^1.13.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.6.3"
  }
}
