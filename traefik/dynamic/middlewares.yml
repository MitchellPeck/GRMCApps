http:
  middlewares:
    hub-forward-auth:
      forwardAuth:
        address: "http://hub:3000/auth/verify"
        trustForwardHeader: true
        authResponseHeaders:
          - X-Auth-User-Id
          - X-Auth-Email
          - X-Auth-Name
          - X-Auth-Roles
